import style from "./header.module.css"
const Header =()=>{
    return(
        <header>
            <h1 className={style.neeraj}>
                header
            </h1>
        </header>
    )
}

export default Header;